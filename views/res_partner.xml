<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_partner_form_inherit_cheque" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.cheque</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="category_id" position="after">
                <field name="weighted_avg_due_date" string="Weighted Average Due Date" readonly="1"/>
            </field>
        </field>
    </record>
</odoo>
